@model Login
@{
	Layout = "_AuthLayout";
	ViewData["Title"] = "Login";
}
<section>
	<a href="/" class="secondary">
		← back to home page
	</a>
	<article>
		<h1>Login to your account</h1>
		<form asp-action="Login" asp-controller="Auth" method="post">
			@Html.ValidationSummary()
			<div>
				<label asp-for="@Model.Email">
					Email address
				</label>
				<input asp-for="@Model.Email" type="email" autocomplete="email" required />
				<small>@Html.ValidationMessageFor(m => m.Email)</small>
			</div>
			<div>
				<label asp-for="@Model.Password">
					Password
				</label>
				<input asp-for="@Model.Password" type="password" autocomplete="current-password" required />
				<small>@Html.ValidationMessageFor(m => m.Password)</small>
			</div>
			<fieldset>
				<label asp-for="@Model.RememberMe" class="inline-flex items-center">
					Remember me
					<input asp-for="@Model.RememberMe" role="switch" type="checkbox">
				</label>
			</fieldset>
			<div class="grid">
				<button type="submit">
					Login
				</button>
			</div>
		</form>
		<footer>
			<a href="/register" class="secondary">
				Don't have an account? Sign up
			</a>
		</footer>
	</article>
</section>
